<div class="card__difficulty card__difficulty--diff{{diff}} card__difficulty--reverse"></div>
<div class="card__textcontainer" id="answerside">
  <div class="card__text {{#if long}}card__text--long{{/if}}">
    {{#each aText}}
      <p>{{this}}</p>
    {{/each}}
  </div>
</div>